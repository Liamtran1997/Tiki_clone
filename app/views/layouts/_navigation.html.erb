<!-- Navigation-->

    <nav class="navbar navbar-expand-lg navbar-light bg-secondary">
      <div class="container px-4 px-lg-5">
        <%= link_to image_tag("logo1.png", size: "200x120"), root_path %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

          <ul class="nav navbar-nav">
            <li>
              <form action="" class="navbar-form">
                <div class="form-group">
                  <div class="input-group">
                    <%= search_form_for @search, url: search_products_path, html: { method: :post } do |f| %>
                      <%= f.search_field :name_cont, placeholder: 'Search Anything Here...' %>
                      <%= button_tag type: 'submit', class: "button my-button" do %>
                        <i class="glyphicon glyphicon-search" aria-hidden="true"></i>
                      <% end %>
                    <% end %>
                  </div>
                </div>
              </form>
            </li>
          </ul>

          <ul class="nav navbar-nav navbar-right"
              id="bs-example-navbar-collapse-1">

            <% if user_signed_in? %>

              <li class="nav-item dropdown">
                <% if current_user.admin == true %>
                  <div class="dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      Create Stuff
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                      <a class="dropdown-item" href="<%= new_product_path %>">Create New Product</a>
                      <a class="dropdown-item" href="<%= new_category_path %>">Create New Category</a>
                    </div>
                  </div>
                <% end %>
              </li>

              <li class="nav-item dropdown">
                <div class="dropdown">
                  <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink"
                     data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="glyphicon glyphicon-user"></span>
                    <%= current_user.name %>
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item"><%= link_to "Update Profile", edit_user_registration_path, :class => "btn" %></a>
                    <a class="dropdown-item"><%= link_to "Checkout", destroy_user_session_path,
                                                         method: :delete, :class => "btn" %></a>
                  </div>
                </div>
              </li>
            <% else %>
              <p class="control">
                <%= link_to "Sign In", new_user_session_path, class: "navbar-item button" %>
              </p>
              <p class="control">
                <%= link_to "Sign up", new_user_registration_path, class: "navbar-item button" %>
              </p>
            <% end %>

            <%= link_to cart_path(@cart), class: "btn btn-outline-dark" do %>
              <i class="bi-cart-fill me-1"></i>
              <span class="icon is-small">
              </span>
              <span>Cart
                <%= cart_count_over_one %>
            </span>
            <% end %>

          </ul>


        </div>
      </div>
    </nav>
